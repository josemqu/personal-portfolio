---
// se ejecuta en el servidor o en build time <----
const { title } = Astro.props
---

<section>
  {title && <h2>{title}</h2>}
  <slot />
</section>

<script>
  // se ejecuta en el cliente <----
</script>

<style>
  section * {
    max-width: 700px;
    margin: 0 auto 1rem;
    /* transition: all 0.2s ease-in-out; */
    transition: all 0.5s linear 0.4s !important;

  }

  h2 {
    margin-bottom: 8px;
    font-weight: 400;
    line-height: 1.5;
    font-size: 1.5rem;
    color: var(--bold-text);
  }

  @media (width <= 700px) {
    section {
      margin-bottom: 38px;
    }
  }
</style>
