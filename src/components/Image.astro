---
import { basics } from "@cv"
const { name, image } = basics

---

<img transition:name="profile" src={image} alt={name} />

<style>
  
  img {
    aspect-ratio: 1 / 1;
    margin: 1rem 0;
  }
  
</style>